<!--
  Merge into android/app/src/main/AndroidManifest.xml after `npx cap add android`.
  Place permissions at the root <manifest> level (alongside the existing INTERNET line).
  Add the application attributes to the existing <application> tag.
-->

<!-- Permissions -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.VIBRATE" />
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.CAMERA" />

<!-- Camera feature (optional so APK still installs on devices without camera) -->
<uses-feature android:name="android.hardware.camera" android:required="false" />

<!--
  Application tag — add these attributes to the <application ...> opening tag:

    android:usesCleartextTraffic="false"
    android:hardwareAccelerated="true"
    android:largeHeap="true"
-->
